syntax = "proto3";

package bdim.comet;
option go_package = ".;grpc";

message Package {
  int32 op = 1;
  int32 roomid = 2;
  bytes body = 3;
}

message PushReply {}

service Comet {
  rpc Push(Package) returns(PushReply);
}